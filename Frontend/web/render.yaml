services:
  - type: web
    name: plagismart-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    headers:
      - key: "X-Frame-Options"
        value: "DENY"
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
        status: 200